  {/* <div
            className="ec-cards-wrap"
            style={{
              position: 'absolute',
              top: '50%',
              right: '-60px',
              left: 'auto',
              width: '900px',
              height: '1000px',
              transform: `translateY(calc(-50% + ${containerTranslateYOffset}px))`,
              transition: 'none',
              opacity: 1,
            }}
          >
            {/* Floating Mention Bubbles */}
          <div
            className="mention-bubble bubble-orange b-howard"
            style={{
              position: 'absolute',
              top: '-10px',
              left: '40px',
              transform: `scale(${bubbleScale})`,
              opacity: bubbleScale,
              transition: 'none',
            }}
          >
            @howard
          </div>
          <div
            className="mention-bubble bubble-dark b-robin"
            style={{
              position: 'absolute',
              top: '90px',
              left: '500px',
              transform: `scale(${bubbleScale})`,
              opacity: bubbleScale,
              transition: 'none',
            }}
          >
            @robin
          </div>

          {/* Stacking & Fanning Cards in exact original z-index and coordinates */}
          {/* 1. c4 (Red face) */}
          <div
            className="ec-card c4"
            style={{
              width: `${card1Width}px`,
              height: `${card1Height}px`,
              left: `${card1Left}px`,
              top: `${card1Top}px`,
              transform: `rotate(${card1Rot}deg)`,
              transition: 'none',
              zIndex: 1,
            }}
          ></div>
          {/* 2. c2 (Colorful New) */}
          <div
            className="ec-card c2"
            style={{
              width: `${card2Width}px`,
              height: `${card2Height}px`,
              left: `${card2Left}px`,
              top: `${card2Top}px`,
              transform: `rotate(${card2Rot}deg)`,
              background: 'linear-gradient(145deg, #ff9ff3, #cc44cc)',
              transition: 'none',
              zIndex: 2,
            }}
          ></div>
          {/* 3. c1 (Tesla newspaper) */}
          <div
            className="ec-card c1"
            style={{
              width: `${card3Width}px`,
              height: `${card3Height}px`,
              left: `${card3Left}px`,
              top: `${card3Top}px`,
              transform: `rotate(${card3Rot}deg)`,
              transition: 'none',
              zIndex: 3,
            }}
          ></div>
          {/* 4. c7 (Van Gogh) */}
          <div
            className="ec-card c7"
            style={{
              width: `${card4Width}px`,
              height: `${card4Height}px`,
              left: `${card4Left}px`,
              top: `${card4Top}px`,
              transform: `rotate(${card4Rot}deg)`,
              transition: 'none',
              zIndex: 4,
            }}
          ></div>
          {/* 5. c10 (Yellow box) */}
          <div
            className="ec-card c10"
            style={{
              width: `${card5Width}px`,
              height: `${card5Height}px`,
              left: `${card5Left}px`,
              top: `${card5Top}px`,
              transform: `rotate(${card5Rot}deg)`,
              transition: 'none',
              zIndex: 5,
            }}
          ></div>
      </div> */}